[tool.poetry]
name = "arguequery"
version = "1.0.0"
description = ""
authors = ["Mirko Lenz <info@mirko-lenz.de>"]

[tool.poetry.dependencies]
python = ">=3.10, <3.13"
nlp-service = "^1.3"
pyyaml = "^6.0"
arg-services = "^1.3"
arguebuf = "^2.0"
typed-settings = { version = "^23.1", extras = ["attrs", "cattrs"] }
rich-click = "^1.7"

[build-system]
requires = ["poetry"]
build-backend = "poetry.masonry.api"
