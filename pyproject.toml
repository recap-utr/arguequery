[tool.poetry]
name = "arguequery"
version = "1.0.0"
description = ""
authors = ["Mirko Lenz <info@mirko-lenz.de>"]

[tool.poetry.dependencies]
python = ">=3.9,<3.12"
typer = "^0.7.0"
nlp-service = "^1.2.2"
pyyaml = "^6.0"
arg-services = "^1.1.0"
arguebuf = { version = "^2.0.0b13", allow-prereleases = true }

[build-system]
requires = ["poetry"]
build-backend = "poetry.masonry.api"
