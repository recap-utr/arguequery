[tool.poetry]
name = "arguequery"
version = "1.0.0"
description = ""
authors = ["Mirko Lenz <info@mirko-lenz.de>"]

[tool.poetry.dependencies]
python = ">=3.9,<3.12"
nlp-service = "^1.3.11"
pyyaml = "^6.0"
arg-services = "^1.3.0"
arguebuf = { version = "^2.0.0b23", allow-prereleases = true }
typed-settings = "^23.0.0"
rich-click = "^1.6.1"

[build-system]
requires = ["poetry"]
build-backend = "poetry.masonry.api"
